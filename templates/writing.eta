<% layout("layout.eta") %>

<ul id="writingList">
  <% it.pages.forEach((page) => { %>
    <li>
      <a href="<%= page.path %>"><%= page.title %></a>
      <date><%= page.date %></date>
    </li>
  <% }) %>
</ul>